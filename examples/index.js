class Rect {
  constructor(width, height) { this.width = width; this.height = height; }

  // TODO: Implement actuall algorithm to calculate the area
  getArea() { return 0; }

  // TODO: use the new get,  set stuff from ES6
  getWidth() {
    return this.width;
  }
}
